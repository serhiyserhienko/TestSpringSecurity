spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security_test?useSSL=false&autoReconnect=true
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#logging:
 # level:
 #   ROOT: DEBUG
server:
  port: 8083
#open-in-view: false # -> to enable Lazy fetch strategyprofiling